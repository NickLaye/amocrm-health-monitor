# Development Dockerfile for amoCRM Health Monitor Backend
FROM node:18-alpine

WORKDIR /app

# Install dependencies for sqlite3
RUN apk add --no-cache python3 make g++

# Copy package files
COPY package*.json ./

# Install dependencies
RUN npm ci

# Copy server code
COPY server ./server

# Default port and exposure
ENV PORT=3001
EXPOSE 3001

# Use nodemon for hot reload
CMD ["npm", "run", "dev"]
