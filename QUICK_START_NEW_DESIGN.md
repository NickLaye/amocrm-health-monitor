# 🚀 Быстрый старт нового дизайна

## Вариант 1: Просмотр с тестовыми данными (самый быстрый)

Откройте `client/src/index.js` и измените:

```javascript
// ❌ Было:
import App from './App';

// ✅ Стало:
import App from './App.new';
```

Запустите:
```bash
cd client
npm start
```

Откроется страница с новым дизайном и **захардкоженными примерами данных**.

---

## Вариант 2: Полная интеграция с реальными данными

### Шаг 1: Переименуйте файлы

```bash
cd client/src

# Сохраните старую версию
mv App.js App.old.js

# Активируйте новую версию с данными
mv App.integrated.js App.js
```

### Шаг 2: Запустите приложение

```bash
cd ..
npm start
```

Откроется страница с новым дизайном и **реальными данными из API**.

---

## Вариант 3: Временный тест (без изменения файлов)

### Создайте временный файл `client/src/TestNewDesign.js`:

```javascript
import React from 'react';
import HealthMonitorDashboard from './components/HealthMonitorDashboard';

function TestNewDesign() {
  return <HealthMonitorDashboard />;
}

export default TestNewDesign;
```

### Измените `client/src/index.js`:

```javascript
import TestNewDesign from './TestNewDesign';

root.render(
  <React.StrictMode>
    <TestNewDesign />
  </React.StrictMode>
);
```

### Запустите:

```bash
cd client
npm start
```

---

## 🔄 Возврат к старому дизайну

### Если использовали Вариант 1:

Откройте `client/src/index.js`:

```javascript
// Верните обратно
import App from './App';
```

### Если использовали Вариант 2:

```bash
cd client/src

# Верните старую версию
mv App.js App.integrated.js
mv App.old.js App.js
```

---

## 📋 Что проверить после запуска

✅ **Фон**: Фиолетовый градиент (#7B5FE8 → #9B6BE8)  
✅ **Заголовок**: "amoCRM Health Monitor" белым текстом по центру  
✅ **Верхний ряд**: 3 белые карточки (Статус, Обновление, Период)  
✅ **GET/POST**: 2 карточки с градиентным фиолетовым фоном  
✅ **Сервисы**: 5 белых карточек в ряд  
✅ **График**: Большая карточка с серым фоном и легендой  
✅ **Инциденты**: Карточка с зеленой иконкой  
✅ **Футер**: Белый текст внизу  
✅ **Hover эффекты**: Карточки поднимаются при наведении  

---

## 🎨 Отличия версий

| Файл | Описание |
|------|----------|
| `App.new.js` | Только новый дизайн, **тестовые данные** |
| `App.integrated.js` | Новый дизайн + **реальные данные из API** |
| `HealthMonitorDashboard.jsx` | Чистый UI компонент (тестовые данные) |
| `HealthMonitorDashboardWithData.jsx` | UI + интеграция с props (реальные данные) |

---

## ⚡ Быстрые команды

### Запуск с тестовыми данными:
```bash
cd client
# Отредактируйте src/index.js: import App from './App.new';
npm start
```

### Запуск с реальными данными:
```bash
cd client/src
mv App.js App.old.js && mv App.integrated.js App.js
cd ..
npm start
```

### Возврат к старому:
```bash
cd client/src
mv App.js App.integrated.js && mv App.old.js App.js
cd ..
npm start
```

---

## 🐛 Решение проблем

### Tailwind стили не применяются

Проверьте `client/src/index.css` - должны быть в начале:
```css
@tailwind base;
@tailwind components;
@tailwind utilities;
```

### Ошибка "Module not found: Can't resolve 'date-fns/locale'"

```bash
cd client
npm install date-fns
```

### Шрифт Inter не загружается

Проверьте `client/public/index.html` - должна быть ссылка на Google Fonts:
```html
<link href="https://fonts.googleapis.com/css2?family=Inter:wght@400;500;600;700&display=swap" rel="stylesheet">
```

---

## 💡 Рекомендация

**Начните с Варианта 1** (App.new.js) чтобы быстро оценить дизайн, затем переключитесь на **Вариант 2** (App.integrated.js) для работы с реальными данными.

---

## 📸 Скриншоты

После запуска вы увидите:
- ✨ Современный градиентный фон
- 💎 Pixel-perfect карточки с тенями
- 🎯 Четкая визуальная иерархия
- 📱 Адаптивный дизайн
- ✨ Плавные hover-эффекты

Приятного использования! 🎉

