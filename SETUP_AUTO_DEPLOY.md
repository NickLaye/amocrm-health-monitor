а# 🚀 Настройка автоматического деплоя через GitHub Actions

## ✅ Что уже сделано:

1. ✅ GitHub Actions workflow настроен с автодеплоем
2. ✅ SSH ключ добавлен на сервер
3. ✅ Git репозиторий настроен на сервере

---

## 📝 Что нужно сделать (5 минут):

### Шаг 1: Добавьте Secrets в GitHub

Перейдите в настройки репозитория:
```
https://github.com/NickLaye/amocrm-health-monitor/settings/secrets/actions
```

Нажмите **"New repository secret"** и добавьте **3 секрета**:

---

#### 1️⃣ `SERVER_HOST`
**Name:** `SERVER_HOST`  
**Value:**
```
77.73.71.242
```

---

#### 2️⃣ `SERVER_USER`
**Name:** `SERVER_USER`  
**Value:**
```
root
```

---

#### 3️⃣ `SSH_PRIVATE_KEY`
**Name:** `SSH_PRIVATE_KEY`  
**Value:** (скопируйте содержимое из файла)

**На вашем компьютере выполните:**
```bash
cat ~/.ssh/id_ed25519
```

Скопируйте **весь вывод** (включая строки -----BEGIN и -----END) и вставьте в поле Value.

---

### Шаг 2: Коммитим и пушим workflow

После добавления secrets, я отправлю обновленный workflow на GitHub.

---

## 🎯 Как это будет работать:

### При каждом `git push` в `main`:

1. ✅ **Build** - GitHub Actions соберет и протестирует код
2. ✅ **Deploy** - Автоматически задеплоит на сервер:
   - Подключится по SSH
   - Выполнит `git pull origin main`
   - Установит зависимости
   - Пересоберет фронтенд
   - Перезапустит PM2
   - Перезагрузит Nginx

---

## 📊 Как проверить:

После добавления secrets:
1. Любое изменение кода → `git push`
2. Зайдите в Actions на GitHub
3. Увидите 2 джобы:
   - ✅ **build** (Node 18 и 20)
   - 🚀 **deploy** (автоматический деплой)
4. Через 2-3 минуты изменения будут на сервере!

---

## 🔒 Безопасность:

- ✅ SSH ключ хранится в GitHub Secrets (зашифрован)
- ✅ Доступен только для Actions, не виден в коде
- ✅ Деплой только из ветки `main`
- ✅ Только при push (не при PR)

---

## 🎉 После настройки:

**Вы сможете:**
- Редактировать код локально
- Делать `git push`
- **Через 2-3 минуты видеть изменения на https://amohealth.duckdns.org**
- Никаких ручных действий! 🚀

---

## ⚡ Готово к использованию!

После добавления secrets дайте мне знать, и я отправлю финальный коммит!

