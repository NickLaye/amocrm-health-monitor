# amoCRM Health Monitor - Pixel-Perfect Design

## 📋 Описание

Новый pixel-perfect React компонент для amoCRM Health Monitor Dashboard, созданный согласно детальной дизайн-спецификации.

## 🎨 Дизайн-система

### Цвета

- **Фоновый градиент**: `#7B5FE8 → #9B6BE8` (фиолетовый вертикальный/диагональный градиент)
- **Фон карточек**: `#FFFFFF` (белый с мягкой тенью)
- **Основной текст**: `#111827`
- **Вторичный текст**: `#6B7280`
- **Акцентный фиолетовый**: `#7C3AED` / `#8B5CF6`
- **Успех (зеленый)**: `#3FA856`
- **Фон успеха**: `#E6F6E8`
- **Зона графика**: `#F3F4F6` (очень светлый серый)

### Типографика

- **Шрифт**: Inter (подключен через Google Fonts)
- **Заголовок страницы**: 24px, semibold, белый, по центру
- **Заголовки секций**: 20px, semibold
- **Большие метрики** (GET/POST): 32px, bold
- **Метрики в карточках**: 22px, semibold
- **Лейблы**: 13px, regular, вторичный цвет
- **Основной текст**: 14px

### Скругления

- **Большие карточки**: 16px
- **Маленькие карточки**: 12px

### Тени

- **Обычное состояние**: `0 4px 18px rgba(0, 0, 0, 0.05)`
- **При наведении**: `0 6px 20px rgba(0, 0, 0, 0.08)` + сдвиг вверх на 2px

### Отступы

- **Вертикальные промежутки между секциями**: 32px (8 spacing units)
- **Внутренние отступы карточек**: 20-24px
- **Промежутки между карточками в ряду**: 16px

## 📦 Структура страницы

1. **Заголовок страницы** - "amoCRM Health Monitor" (белый текст над градиентом)
2. **Верхний ряд статуса** - 3 карточки:
   - Общий статус (с зеленой иконкой)
   - Последнее обновление
   - Селектор периода
3. **Среднее время ответа API** - 2 большие карточки (GET и POST) с градиентным фоном
4. **Метрики по сервисам** - 5 карточек в ряд:
   - API (GET)
   - API (POST)
   - Веб-интерфейс
   - Вебхуки
   - Digital Pipeline (с легкой подсветкой)
5. **График времени ответа** - большая карточка с графиком и легендой
6. **История инцидентов** - карточка с зеленой иконкой и текстом
7. **Футер** - статусная строка белым текстом с прозрачностью

## 🚀 Использование

### Вариант 1: Просмотр в отдельном режиме

Для просмотра нового дизайна создайте временный файл для тестирования:

```bash
# В директории client/src создайте TestApp.js
```

```javascript
import React from 'react';
import HealthMonitorDashboard from './components/HealthMonitorDashboard';

function TestApp() {
  return <HealthMonitorDashboard />;
}

export default TestApp;
```

Затем временно измените `index.js`:

```javascript
import TestApp from './TestApp';
// вместо: import App from './App';

root.render(
  <React.StrictMode>
    <TestApp />
  </React.StrictMode>
);
```

### Вариант 2: Замена основного компонента

Чтобы заменить текущий дизайн на новый:

```bash
# Переименуйте текущий App.js
mv src/App.js src/App.old.js

# Переименуйте новый App.js
mv src/App.new.js src/App.js

# Запустите приложение
npm start
```

### Вариант 3: Интеграция с существующим кодом

Добавьте новый компонент в существующий Dashboard:

```javascript
import HealthMonitorDashboard from './components/HealthMonitorDashboard';

// В вашем App.js или Dashboard.js
<HealthMonitorDashboard />
```

## ✨ Особенности

### Адаптивность

- **Desktop** (1240px+): все элементы в один ряд, полный размер
- **Tablet** (768-1239px): карточки сервисов переходят в 2-3 в ряд
- **Mobile** (<768px): все карточки в столбик, стек

### Интерактивность

- **Hover эффекты**: все карточки поднимаются на 2px и усиливается тень
- **Transitions**: плавные переходы 200ms
- **Focus states**: кастомные стили для селектора периода

### Выделение особых состояний

- **Digital Pipeline**: карточка имеет легкую теплую подсветку (`rgba(255, 200, 0, 0.04)`) если время ответа аномально высокое

## 🎯 Точность выполнения спецификации

✅ Фиолетовый градиентный фон  
✅ Белые карточки с тенями и скруглениями  
✅ Правильная типографика (размеры, веса, выравнивание)  
✅ Общая baseline для чисел и единиц измерения  
✅ Одинаковая высота карточек в рядах  
✅ Консистентные отступы (8px scale)  
✅ Hover эффекты с elevation  
✅ Адаптивный дизайн  
✅ Семантический HTML  
✅ Использование Tailwind CSS  

## 🔧 Технические детали

- **React**: Функциональный компонент
- **Styling**: Tailwind CSS utility classes
- **Responsiveness**: Grid layout с breakpoints
- **Accessibility**: Семантические теги, ARIA labels
- **Performance**: Оптимизированные transitions

## 📝 Примечания

- Все значения в компоненте захардкожены как плейсхолдеры
- Для подключения реальных данных интегрируйте с существующими хуками (`useMonitoring`)
- График пока представлен плейсхолдером (для интеграции Chart.js используйте существующий `ResponseTimeChart`)

## 🎨 Кастомизация

Все цвета и настройки определены в `tailwind.config.js`:

```javascript
colors: {
  purple: {
    start: '#7B5FE8',
    end: '#9B6BE8',
    accent: '#7C3AED',
    light: '#8B5CF6',
  },
  success: {
    DEFAULT: '#3FA856',
    bg: '#E6F6E8',
  },
}
```

Для изменения цветовой схемы отредактируйте эти значения.

